const STRING_BOOLEANS_MAP = {
    TRUE: 'true',
    FALSE: 'false',
};

const STRING_BOOLEANS = Object.values(STRING_BOOLEANS_MAP);

const UUID_LENGTH = 36;

const ENTITIES = {
    CAR: 'car',
    TRAILER: 'trailer',
    DRIVER: 'driver',
};

module.exports = {
    STRING_BOOLEANS_MAP,
    STRING_BOOLEANS,
    UUID_LENGTH,
    ENTITIES,
};
