const STRING_BOOLEANS_MAP = {
    TRUE: 'true',
    FALSE: 'false',
};

const STRING_BOOLEANS = Object.values(STRING_BOOLEANS_MAP);

module.exports = {
    STRING_BOOLEANS_MAP,
    STRING_BOOLEANS,
};
