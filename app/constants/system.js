const ROLES = {
    UNCONFIRMED_ROLE1: 'unconfirmed_role1',
    UNCONFIRMED_ROLE2: 'unconfirmed_role2',
    UNCONFIRMED_ROLE3: 'unconfirmed_role3',
    CONFIRMED_PENDING_ROLE1: 'confirmed_pending_role1',
    CONFIRMED_PENDING_ROLE2: 'confirmed_pending_role2',
    CONFIRMED_PENDING_ROLE3: 'confirmed_pending_role3',
    ROLE1: 'role1',
    ROLE2: 'role2',
    ROLE12: 'role12',
    ROLE3: 'role3'
};

const MAP_FROM_UNCONFIRMED_TO_CONFIRMED_ROLE = {
    [ROLES.UNCONFIRMED_ROLE1]: ROLES.CONFIRMED_PENDING_ROLE1,
    [ROLES.UNCONFIRMED_ROLE2]: ROLES.CONFIRMED_PENDING_ROLE2,
    [ROLES.UNCONFIRMED_ROLE3]: ROLES.CONFIRMED_PENDING_ROLE3,
};

const MAP_FROM_PENDING_ROLE_TO_MAIN = {
    [ROLES.CONFIRMED_PENDING_ROLE1]: ROLES.ROLE1,
    [ROLES.CONFIRMED_PENDING_ROLE2]: ROLES.ROLE2,
    [ROLES.CONFIRMED_PENDING_ROLE3]: ROLES.ROLE3,
};


const UNCONFIRMED_EMAIL_ROLES = [
    ROLES.UNCONFIRMED_ROLE1,
    ROLES.UNCONFIRMED_ROLE2,
    ROLES.UNCONFIRMED_ROLE3,
];

const ROLES_TO_REGISTER = [
    ROLES.ROLE1, ROLES.ROLE2, ROLES.ROLE3,
];

const MAP_ROLES_FROM_CLIENT_TO_SERVER = {
    [ROLES.ROLE1]: ROLES.UNCONFIRMED_ROLE1,
    [ROLES.ROLE2]: ROLES.UNCONFIRMED_ROLE2,
    [ROLES.ROLE3]: ROLES.UNCONFIRMED_ROLE3,
};

const PERMISSIONS = {
    READ_USERS: 'read_users',
    EDIT_USERS: 'edit_users',
    UPDATE_USERS: 'update_users',
    DELETE_USERS: 'delete_users',
    AUTHORIZATION: 'authorization',
    FINISH_REGISTRATION: 'finish_registration',
    INVITE_WORKER: 'invite_worker',
    BECOME_ROLE_1: 'become_role_1',
    BECOME_ROLE_2: 'become_role_2',
    RESET_PASSWORD: 'reset_password',
    CONFIRM_EMAIL: 'confirm_email',
};

const ROLES_WITH_PERMISSIONS = [
    [ROLES.UNCONFIRMED_ROLE1, [
        PERMISSIONS.CONFIRM_EMAIL,
    ]],
    [ROLES.UNCONFIRMED_ROLE2, [
        PERMISSIONS.CONFIRM_EMAIL,
    ]],
    [ROLES.UNCONFIRMED_ROLE3, [
        PERMISSIONS.CONFIRM_EMAIL,
    ]],
    [ROLES.CONFIRMED_PENDING_ROLE1, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.FINISH_REGISTRATION,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.CONFIRMED_PENDING_ROLE2, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.FINISH_REGISTRATION,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.CONFIRMED_PENDING_ROLE3, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.FINISH_REGISTRATION,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.ROLE1, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.INVITE_WORKER,
        PERMISSIONS.BECOME_ROLE_2,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.ROLE2, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.INVITE_WORKER,
        PERMISSIONS.BECOME_ROLE_1,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.ROLE12, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.INVITE_WORKER,
        PERMISSIONS.RESET_PASSWORD,
    ]],
    [ROLES.ROLE3, [
        PERMISSIONS.AUTHORIZATION,
        PERMISSIONS.RESET_PASSWORD,
    ]],
];

const FILE_LABELS = {
    PASSPORT: 'passport',
    PLAN: 'plan',
    REPORT: 'report',
};

module.exports = {
    ROLES,
    ROLES_TO_REGISTER,
    PERMISSIONS,
    ROLES_WITH_PERMISSIONS,
    MAP_ROLES_FROM_CLIENT_TO_SERVER,
    MAP_FROM_UNCONFIRMED_TO_CONFIRMED_ROLE,
    MAP_FROM_PENDING_ROLE_TO_MAIN,
    UNCONFIRMED_EMAIL_ROLES,
    FILE_LABELS,
};
